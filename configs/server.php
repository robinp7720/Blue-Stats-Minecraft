<?php
$server_info = array(
	"ip" => "127.0.0.1", /* Server Ip (If hosted on same machine leave as "localhost") */
	"port" => "25565", /* Server Port (25565 is default) */
	"query_enabled" => true, /* Weather or not to enable mc query (If enabled players can see who's online) */
	"server_name" => "MySunland", /* Not required but shows this in the tab bar */
);
