<?php
$config[0]["server"] = array(
	"ip" => "mysunland.org", /* Server Ip (If hosted on same machine leave as "localhost") */
	"port" => "25565", /* Server Port (25565 is default) */
	"QueryPort" => "25565", /* Server Port (25565 is default) */
	"query_enabled" => true, /* Weather or not to ping server (If enabled players can see who's online) */
	"server_name" => "MySunland", /* Not required but shows this in the tab bar */
	"useQuery" => true /* Weather or not to use MC query to retrieve online users */
);
